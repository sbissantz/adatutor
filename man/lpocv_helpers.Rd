% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpocv.R
\name{lpocv_helpers}
\alias{lpocv_helpers}
\alias{lpocv_nested}
\alias{tree_control_names}
\alias{normalize_treehypar}
\alias{fold_score}
\title{Internal Helpers for lpocv()}
\usage{
lpocv_nested(
  formula,
  data,
  group,
  model,
  grid,
  treehypar,
  folds,
  criterion,
  verbose
)

tree_control_names()

normalize_treehypar(treehypar, row = NULL)

fold_score(formula, train, test, model, pars, treehypar = NULL)
}
\arguments{
\item{formula, train, test, model, pars, data, group, folds, criterion, verbose}{Internal arguments; see \code{\link[adatutor]{lpocv}}.}

\item{grid, T, eta, depth, treehypar, row}{Internal arguments; see \code{\link[adatutor]{lpocv}}. \code{row} is one
grid row, whose \code{rpart} controls override the call-level ones.}
}
\value{
\code{normalize_grid()} and \code{normalize_treehypar()} return
lists; \code{fold_score()} returns a numeric vector.
}
\description{
Not exported. \code{tree_control_names()} lists the
\code{\link[rpart]{rpart.control}} parameters a grid column may set,
\code{normalize_grid()} fills in the columns a grid omits and warns about
any that set nothing, \code{normalize_treehypar()} builds the single tree
control every tree-based learner shares and is the one place a per-row
control is merged into it, \code{fold_score()} fits one learner on one fold
and returns its continuous score for the held-out rows, and
\code{lpocv_nested()} runs the outer loop, calling \code{lpocv()} itself for
each inner search so the fold logic exists in one place.
}
\keyword{internal}
