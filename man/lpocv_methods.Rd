% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lpocv.R
\name{lpocv_methods}
\alias{lpocv_methods}
\alias{print.lpocv}
\alias{summary.lpocv}
\alias{as.data.frame.lpocv}
\alias{plot.lpocv}
\title{Methods for lpocv Objects}
\usage{
\method{print}{lpocv}(x, metric = "auroc", ...)

\method{summary}{lpocv}(object, metric = "auroc", ...)

\method{as.data.frame}{lpocv}(x, ...)

\method{plot}{lpocv}(
  x,
  metric = "auroc",
  ci = NULL,
  levels = c(0.5, 0.8, 0.95),
  baseline = "auto",
  drop_flag = 0.05,
  ...
)
}
\arguments{
\item{x, object}{An object of class \code{"lpocv"}.}

\item{metric}{The measure to display. Defaults to \code{"auroc"}.}

\item{...}{Ignored.}

\item{ci}{Optional. A named list of \code{\link[adatutor]{bootCI}} results,
one per project, which adds interval bands. Without it the plot shows point
estimates only.}

\item{levels}{The band widths, widest last. Defaults to 0.50, 0.80 and 0.95.}

\item{baseline}{The reference line. \code{"auto"} puts it where a worthless
model would score, which is not the same height for every measure: 0.5 for
\code{auroc} and \code{bacc}, 0 for \code{mcc}, and each project's own base
rate for the prevalence-dependent ones. A number overrides it; \code{NA}
omits the line.}

\item{drop_flag}{Projects whose bootstrap rejected more than this share of
attempts get a starred axis label and a footnote. Defaults to 0.05.}
}
\value{
\code{print()} returns \code{x} invisibly; the others return data
frames.
}
\description{
\code{print()} shows the per-fold estimates for the primary
measure and \code{summary()} returns them as a data frame.
\code{as.data.frame()} flattens the whole \code{setting x project x metric}
array into one long table, and is what the other two read from, so the
array is the storage and these are views of it.
}
