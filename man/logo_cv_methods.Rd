% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/logo-cv.R
\name{logo_cv_methods}
\alias{logo_cv_methods}
\alias{print.logo_cv}
\alias{summary.logo_cv}
\alias{as.data.frame.logo_cv}
\alias{plot.logo_cv}
\title{Methods for logo_cv objects}
\usage{
\method{print}{logo_cv}(x, metric = "auroc", ...)

\method{summary}{logo_cv}(object, metric = "auroc", ...)

\method{as.data.frame}{logo_cv}(x, ...)

\method{plot}{logo_cv}(
  x,
  metric = "auroc",
  levels = c(0.5, 0.8, 0.95),
  baseline = "auto",
  max_rejected = 0.05,
  ...
)
}
\arguments{
\item{x, object}{An object of class \code{logo_cv}.}

\item{metric}{The measure to show. Defaults to \code{"auroc"}.}

\item{...}{Ignored.}

\item{levels}{The band widths, widest last. Defaults to 0.50, 0.80 and
0.95.}

\item{baseline}{The reference line. \code{"auto"} draws it where a useless model
would score: 0.5 for \code{auroc} and \code{bacc}, 0 for \code{mcc}, and each project's
base rate for the others. A number sets it; \code{NA} leaves it out.}

\item{max_rejected}{Projects whose bootstrap rejected more than this share
of draws get a star and a footnote. Defaults to 0.05.}
}
\value{
\code{print()} and \code{plot()} return \code{x} invisibly; \code{summary()} and
\code{as.data.frame()} return data frames.
}
\description{
\code{print()} shows the per-project estimates of one measure, \code{summary()}
returns them as a data frame, and \code{as.data.frame()} flattens all estimates
into one long table. \code{plot()} draws the estimates per project, with bands
when \code{x} carries intervals from \code{\link[=bootstrap]{bootstrap()}}.
}
\seealso{
Other cross-validation:
\code{\link[=bootstrap]{bootstrap()}},
\code{\link[=logo_cv]{logo_cv()}},
\code{\link[=partition]{partition()}}
}
\concept{cross-validation}
