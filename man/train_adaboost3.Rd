% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train_adaboost3.R
\name{train_adaboost3}
\alias{train_adaboost3}
\title{Train an AdaBoost Model}
\usage{
train_adaboost3(
  formula,
  data,
  T,
  eta,
  tree_hyperpar,
  input_checks = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{formula}{A symbolic description of the model to be fitted (e.g.,
\code{response ~ predictors}).}

\item{data}{A data frame containing the variables in the model.}

\item{T}{An integer specifying the number of boosting iterations.}

\item{eta}{A numeric value representing the learning rate of the algorithm.}

\item{tree_hyperpar}{A list of control parameters for the decision tree
learner (passed to \code{rpart::rpart} as the \code{control} argument).}

\item{input_checks}{A logical value indicating whether to perform input
validation checks. Defaults to \code{TRUE}.}

\item{verbose}{A logical value indicating whether to display verbose output
during the training process. Defaults to \code{TRUE}.}
}
\value{
A list containing the trained weak learners (\code{h}) and their associated weights (\code{a}).
Additional attributes may be included for model tracking purposes.
}
\description{
This function trains an AdaBoost model using decision trees as weak learners.
The implementation allows for customization of the number of iterations,
learning rate, and decision tree hyperparameters. It also provides verbose
output to track the training progress.
}
\details{
The function implements the AdaBoost algorithm with a specified
number of iterations (\code{T}). It initializes observation weights, trains a
sequence of decision trees, and updates the weights at each iteration based
on prediction errors. A final ensemble of weak learners is produced.

Key steps in the algorithm:
\enumerate{
\item Initialize observation weights.
\item Train a decision tree using the current weights.
\item Compute the weighted classification error and update the observation
weights.
\item Store the weak learner and its associated weight.
}
}
\examples{
\dontrun{
# Example usage:
library(rpart)
data(iris)
# Prepare binary classification data
iris_binary <- iris[iris$Species != "setosa", ]
iris_binary$Species <- factor(iris_binary$Species)

# Set tree hyperparameters
tree_control <- rpart.control(maxdepth = 1, cp = 0)

# Train AdaBoost model
model <- train_adaboost3(Species ~ ., data = iris_binary, T = 10,
                         eta = 0.5, tree_hyperpar = tree_control)
}

}
