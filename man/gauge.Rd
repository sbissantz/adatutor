% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gauge.R
\name{gauge}
\alias{gauge}
\alias{plot.gauge}
\title{Gauge the importance of each predictor}
\usage{
gauge(fit)

\method{plot}{gauge}(x, top_n = 15, ...)
}
\arguments{
\item{fit}{A fit from \code{\link[=adaboost]{adaboost()}}.}

\item{x}{A \code{gauge} object from \code{gauge()}.}

\item{top_n}{The largest number of predictors to plot. Defaults to 15.}

\item{...}{Ignored.}
}
\value{
\code{gauge()} returns a data frame of class \code{gauge} with the columns
\code{variable} and \code{importance} (a percentage), sorted from most to least
important. \code{plot()} draws a bar chart and returns \code{NULL} invisibly.
}
\description{
Sums, for each predictor, the model weights of the trees that split on it,
and scales the sums to 100. A stump gives its whole weight a_t to its one
variable. A deeper tree splits its weight across its variables by rpart's
improvement measure. A tree that never split adds nothing.
}
\section{What it does not tell you}{

The numbers are relative: they show how the ensemble divided its weight,
not what a variable is worth on its own. They have no sign, correlated
predictors share their weight, and the ranking changes with the
hyperparameters and the training data.
}

\examples{
data(altmejd)
prednms <- c("power.o", "effect_size.o", "n.o", "p_value.o")

h <- rpart::rpart(
  replicate ~ .,
  data = altmejd[, c(prednms, "replicate")],
  maxdepth = 1,
  model = TRUE
)
fit <- adaboost(h, n_iter = 50, eta = 1, verbose = FALSE, input_checks = FALSE)

imp <- gauge(fit)
imp

plot(imp)

}
\seealso{
Other tutorial plots:
\code{\link{tutplot}},
\code{\link[=tutplot_boundary]{tutplot_boundary()}},
\code{\link{tutplot_opts}},
\code{\link[=viridis_tree]{viridis_tree()}}
}
\concept{tutorial plots}
