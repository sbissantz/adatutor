% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootCI.R
\name{bootCI}
\alias{bootCI}
\title{Bootstrap Confidence Intervals for Classification Performance}
\usage{
bootCI(
  actual,
  score,
  n_resample = 1000,
  stratified = FALSE,
  conf = 0.95,
  threshold = 0,
  k = NULL
)
}
\arguments{
\item{actual}{The true labels, in any encoding
\code{\link[adatutor]{assess}} accepts.}

\item{score}{A numeric vector of continuous scores, one per observation.}

\item{n_resample}{The number of \emph{usable} resamples to collect. Defaults
to 1000.}

\item{stratified}{A logical value. \code{FALSE} (the default) resamples the
fold as a whole; \code{TRUE} resamples each class separately and preserves
the class counts. See Details.}

\item{conf}{The width of the interval. Defaults to 0.95.}

\item{threshold, k}{Passed to \code{\link[adatutor]{assess}}.}
}
\value{
A data frame with one row per measure and the columns \code{metric},
\code{estimate} (from the observed data, not the resamples), \code{lower},
\code{upper} and \code{drop}. The attributes \code{n_resample},
\code{stratified}, \code{conf} and \code{notes} record how it was produced.
}
\description{
Resamples the held-out cases of a single fold to attach an
interval to every measure \code{\link[adatutor]{assess}} reports. The model
is held fixed: only the test set is resampled, so the interval answers "how
much would this estimate move if this project had drawn different studies?"
}
\details{
Each resample is scored with one call to \code{\link[adatutor]{assess}}, so all
measures come from the same draws. That keeps the intervals coherent -- the
\code{auroc} and \code{mcc} intervals for a project describe the same
resampled sets rather than two independent runs -- and it makes the whole
bundle cost little more than a single measure.

\strong{Rejected resamples.} A resample can come out with only one class in
it, and \code{auroc} and \code{auprc} are then undefined. Those draws are
rejected and redrawn, so \code{n_resample} is the number of \emph{usable}
resamples rather than the number attempted. The count of rejections is
returned as \code{drop}, and it is not a footnote: on a fold of 13 studies
with only 2 failures, roughly \eqn{(11/13)^{13} \approx 11\%} of draws contain
no failure at all. Redrawing buys precision, not correctness -- the surviving
resamples are conditioned on both classes appearing, which trims exactly the
lopsided draws a tail is made of. An interval from a fold with a high
\code{drop} is narrower than it should be, and an upper bound of 1 there is an
artifact of that conditioning rather than a finding.

\strong{Stratified resampling.} With \code{stratified = TRUE} the two classes
are resampled separately, so every draw keeps the fold's class counts and
nothing is ever rejected. The cost is that the base rate no longer varies
between resamples. That suits \code{auroc} and \code{bacc}, which do not
depend on prevalence, and understates the uncertainty of \code{auprc},
\code{ppv}, \code{npv}, \code{f1}, \code{acc} and \code{mcc}, which do. Since
one call produces every measure from shared draws, both cannot be right at
once; the default keeps the prevalence-dependent measures honest.
}
\examples{
data(altmejd)
prednms <- c("power.o", "effect_size.o", "n.o", "p_value.o")
train <- altmejd[altmejd$pid != "ssrp", ]
test <- altmejd[altmejd$pid == "ssrp", ]

fit <- adaboost(
  replicate ~ .,
  data = train[, c(prednms, "replicate")],
  T = 10,
  eta = 1,
  verbose = FALSE,
  input_checks = FALSE
)
margin <- predict(
  fit,
  test[, prednms],
  type = "margin",
  verbose = FALSE,
  input_checks = FALSE
)

set.seed(1)
ci <- bootCI(test$replicate, margin, n_resample = 200)
ci[ci$metric \%in\% c("auroc", "auprc", "bacc"), ]

}
